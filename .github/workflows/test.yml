name: Test Greeting Action
on: push

jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./study-1 #make sure pathing is correct, ./ is root
        with:
          who-to-greet: 'Claude'
