#this is the actual workflow, the one that triggers and defines job/runner
name: Test Greeting Action
on:
  #push:
  workflow_dispatch:
  # workflow_dispatch:
  #   inputs:
  #     who_to_greet:
  #       description: 'Who to greet'
  #       required: true
  #       default: 'World'

jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./study-1 #make sure pathing is correct, ./ is root
        with:
          who_to_greet: 'Claude'
